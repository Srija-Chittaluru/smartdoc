# Copy this file to .env and fill in your key.
#   cp .env.example .env
#
# .env is git-ignored, so your key never reaches the repository.

OPENAI_API_KEY=sk-your-key-here

# Optional overrides
CHAT_MODEL=gpt-4o-mini
API_HOST=127.0.0.1
API_PORT=8000
